nav파일을 professor와 student로 나눠서 메뉴를 다시 만들었습니다.
성적확인

check_score.html(메인) -> check_score.php(확인)
학생으로 로그인되어야만 실행이 됩니다



성적부여

get_score.html(메인) -> get_subject.php(교과목조회) -> get_score.php(과목선택) -> score.php(성적부여)

get_subject와 get_score.php의 버튼이 밑에 있는 이유는

form이 같은 div안에 있어야 post로 옮겨져서 임의로 오른쪽으로 배치하였습니다.

score.php의 쿼리는 Class에 LID(과목코드), SID(학번)이 있어야 실행됩니다


로그인시 로그아웃부분이 숨는것을 해결못했습니다..